# STATUS — 2026-09-08（阶段 2B 完成交叉比对 · 停在阶段 2 · 缺口记录）

- **已完成到哪**：
  - 获得两份独立抓包证据：
    1. `baseline-2026-09-08-01.pcapng`（43828 字节，704 包，sha256 `2fe44431...`）
    2. `baseline-2026-09-08-02.pcapng`（26380 字节，400 包，sha256 `3f3fd740...`）
  - CRC 回归验证：第二会话 59/59 个非空载荷帧原样命中，覆盖 19 种长度；两会话累计 212/212 帧 100% 命中，CRC-16/ARC 参数完全固化。
  - 假设 B（ACK 回显机制）：**已消灭**。双会话实证 Host 发往 Band 的 ACK 100% 逐帧回显；Band 发往 Host 存在稳定的累积确认机制（在密集突发包时跳过中间 seq 回发高位 seq）。
  - 假设 C（前导帧归属）：**已消灭**。双会话实证 #112/#114 与 #108/#110 为 100% 恒定字节的 SPP 链路初始化交互（11B 与 14B），全程无其他出现，且未包裹 A5A5 帧。
  - 测试向量：已在 `core/tests/fixtures/` 固化两份真实帧测试向量（协商帧 `frame-type02-2026-09-08.bin` 与数据帧 `frame-type03-2026-09-08-02.bin`）。
- **卡在哪一步**：假设 A 未完全消灭。
  - 缺口 1：`type=0x02` 协商帧在两次会话中 22 字节载荷 100% 完全相同（无任何字节变动），缺乏变量跳变证据以完全排除其他 TLV 切分边界。
  - 缺口 2：`type=0x03` 业务数据帧宏观分为握手组 `0x0101`（4 帧定长）与业务组 `0x0102`（动态变长），但 `01 02` 后续内部具体字段/加密格式尚未逆向。
  - 准入规则判定：按 PHASE2B-BRIEF.md 规定，A 未完全消灭，**严禁进入阶段 3，停留在阶段 2**。
- **需要人做什么**：
  - 审阅当前阶段 2B 取证报告与 `docs/protocol/transport.md`。
  - 决策后续路径：是继续在阶段 2 进行上层快应用流量分析以消灭 A 中业务载荷细节，还是裁定当前传输层（帧边界、长度、CRC、ACK、首包方向）已满足传输帧编解码要求并放行阶段 3。
- **下一条命令**：等待用户指令；若批准进入阶段 3，将基于已确认的帧边界、CRC-16/ARC、type/seq/len 布局及测试向量编写 `core/src/crc.rs` 与 `core/src/frame.rs`。
