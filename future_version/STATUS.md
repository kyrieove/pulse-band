# STATUS — 2026-09-08（阶段 5 停在离线认证分析 · 结构已整理，算法仍未确认）

- **已完成到哪**：
  - 阶段 1~4 完成并已推送；阶段 4 真机 RFCOMM 首通成功。
  - 阶段 5 前置凭据已按授权受控导入：`%LOCALAPPDATA%\PulseDev\run\device.json` 存在，authkey 仅确认 16B，未泄露。
  - 新增 `docs/protocol/auth.md`，记录两份 pcapng 中 4 步认证帧的脱敏结构：29B → 63B → 68B → 22B；材料长度分别为 16B、16B+32B、32B+21B、无高熵材料。
  - 结构分析显示 payload 外层为 `01 01`，后续字节形状与 protobuf wire type 一致；**尚不能确认协议就是 protobuf**。
- **当前状态**：阶段 5 停在离线认证分析。尚未连接手环、尚未发包、尚未实现猜测性的 `session.rs`。
- **仍未确认**：
  - authkey 如何参与计算；
  - 16B、32B、21B 材料的真实含义与派生算法；
  - 具体密码学套件；
  - 22B 响应是否只有认证成功才会返回。
- **当前闸门**：不允许连接手环、不允许发送猜测认证包、不允许进入阶段 6。
- **下一步**：等待可信公开协议依据、失败认证对照抓包，或在不连接手环的前提下继续离线结构分析。任何新增结论必须带命令原文、输出摘录、pcapng 路径与 sha256、包号或偏移。

