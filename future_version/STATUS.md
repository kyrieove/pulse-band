# STATUS — 2026-09-08（阶段 5 停在第 1 步离线认证分析 · 认证算法和成功判据仍未确认）

- **已完成到哪**：
  - 阶段 4 完成：真机手环 RFCOMM 首次直连与 4a/4b/4c 首包实测完成，保护条款计数器终值（连接失败 0 次，发包失败 0 次），收尾修正已提交（`3411f6e`）。
  - 阶段 5 凭据与前置闸门：
    1. 进程检查：`Get-Process Pulse,oronbox` 输出为空，确认无竞争进程。
    2. 凭据导入：`device.json` 已按授权受控导入，authkey 仅确认长度 16B（32 hex 字符），addr 存在，未泄露任何凭据原文。
    3. 配对状态：手环在 Windows 注册表 `0434c3979a06` 配对正常。
  - 阶段 5 第 1 步离线认证分析：
    1. 在两份 pcapng（`baseline-2026-09-08-01` 与 `baseline-2026-09-08-02`）中观察到 4 个固定形状的 `type=0x03` 握手帧（公共前缀长度 2B）。
    2. 目前只能确认：
       - 两份成功会话中均出现长度为 22B 的候选认证后响应，CRC 字段为 0xdcf1；响应原始字节按阶段 5 脱敏规则不记录。该响应是否只有认证成功后才返回，仍是假设。
       - 随后进入 `type=0x03，payload 前缀为 0x0102` 业务流，不能单独证明认证算法或成功。
    3. 目前不能确认：
       - authkey 如何参与计算：仍未确认；
       - 32B 和 21B 字段的派生算法：仍未确认；
       - 具体密码学套件：仍未确认；
       - 22B 响应是否只有认证成功才返回：仍是假设。
- **当前状态**：阶段 5 停在第 1 步离线认证分析 · 认证算法和成功判据仍未确认。
- **需要人做什么**：
  - 提供新的协议依据或失败认证对照证据（以闭环密码学套件与认证状态机判定逻辑）。
- **下一步**：当前不允许进入真机认证；需要新的协议依据或失败认证对照证据。
