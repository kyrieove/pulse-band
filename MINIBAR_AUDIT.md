# Pulse 2.0 桌面额度悬浮窗（MiniBar）完整技术审计与实现文档

> 本文档完整记录 Pulse 2.0 桌面额度悬浮组件从设计原则、调用链架构、窗口物理几何、数学轮廓、交互状态机、数据流解析到验证套件的全套技术实现，供架构审查与同行技术评审。

---

## 一、系统定位与工程背景

### 1.1 演进目标
悬浮窗原本是位于桌面横向展示的极简状态条，现已整体重构为：
**“吸附于屏幕左右边缘的黑色竖向侧栏 + 三个 Agent 额度圆环 + 向桌面内侧展开的单项详情卡 + 可收起为边缘微胶囊标签”** 的高级系统级悬浮组件。

视觉与体验对标体系：
- Apple visionOS 悬浮毛玻璃与微妙层次感
- Raycast 紧凑高密度系统级交互
- 高级智能手表表盘刻度环与数据分级排版

### 1.2 工程纪律与范围防爆（严格遵守仓库 AGENTS.md）
- **绝不伪完成 (Zero Fake Completion)**：严禁硬编码百分比、假进度、模拟倒计时。无数据时如实标记为 `—`，第三方官方未提供 7 天额度时标明 `官方未提供此周期额度`，缺失重置时间标明 `重置时间未知`。
- **物理与逻辑边界隔离**：本次改造仅涉及悬浮窗 UI、交互、动效及主进程窗口物理管理；**未触碰** Core 核心、蓝牙底层协议、设备通信协议或额度采集服务。
- **敏感信息绝对隔离**：经过全量正则表达式扫描，敏感词（Token、Secret、Credential、PrivateKey、MAC、DeviceAddress、install.local）绝不流向 Renderer，绝不出主进程。

---

## 二、四层架构与端到端实际调用链

悬浮窗严格遵守 Electron 四层架构隔离：

```text
[Renderer 渲染层]
  MiniBar.tsx, agent-quota-utils.ts
  - 负责 UI 渲染、动效控制、右键菜单、本地背景透明度
  - 严禁直接调用 Node.js API 或操作系统 API
        │
        ▼ (通过 window.codeisland 安全 IPC 桥)
[Preload 桥接层]
  src/preload/index.ts
  - 暴露: getMinibarState, setMinibarExpanded, setMinibarDisplayMode, notifyDragStart, closeMinibar
  - 单向透传，严禁内联业务逻辑
        │
        ▼ (ipcRenderer.send / invoke)
[Main 主进程服务层]
  src/main/minibar-window.ts, src/main/services/minibar-geometry.ts
  - 管理原生 BrowserWindow 物理边界与屏幕跨屏吸附
  - 消费 SessionManager (会话状态) 与 StatusServer (额度数据) 并推送至渲染层
  - 严禁假装拥有物理硬件能力
        │
        ▼ (RPC / Local Socket)
[Core / Hardware 底层]
  pulse-core (Rust) 与手环物理通信
  - 本次改造保持已有稳定状态，未触碰 live.rs 与 session.rs
```

### 实际数据流与字段语义对账

| 数据源 | 核心字段 | 语义与转换规则 | 权威性判定 |
| :--- | :--- | :--- | :--- |
| `StatusServer` (`quotas[agent]`) | `pct5h` / `pct7d` | **已使用百分比** (0~100)。由纯函数 `toRemainingPercent(used)` 转换为**剩余百分比**：`clamp(100 - round(used))`。若为 `null`/`undefined` 则返回 `null`（界面显示 `—`）。 | `authoritative === true` 为官方权威实时数据（绿标 Live）；`false` 为本地估算（橙标 Est）。 |
| `StatusServer` (`quotas[agent]`) | `resetText` / `reset7dText` | 原始英文格式（如 `51min · 18:40`、`4h 22min`、`2d 7h`、`ready`）。由纯函数 `parseResetTimeInfo` 解析为易读中文倒计时与绝对重置时间。 | 严格保真，无钟点信息时绝不伪造虚假时间戳。 |
| `SessionManager` (`sessions`) | `status`, `currentTool` | 当前 Agent 活跃状态（`running_tool`, `thinking`, `idle`）。 | 真实进程活动追踪。 |

---

## 三、窗口物理管理与几何计算系统 (`minibar-geometry.ts`)

为保证窗口展开、收起、跨屏吸附的绝对平滑，所有几何计算由纯函数实现，且 100% 覆盖独立单元测试：

### 3.1 核心尺寸常量
- **收起侧栏态**：宽 `COLLAPSED_WIDTH = 80 DIP`，高 `COLLAPSED_HEIGHT = 340 DIP`
- **向桌面展开态**：宽 `EXPANDED_WIDTH = 370 DIP`，高 `EXPANDED_HEIGHT = 340 DIP`
  - 详情卡宽 264 DIP + 间距/尖角 26 DIP + 侧栏 80 DIP = 370 DIP
- **微型边缘胶囊标签态**：宽 `EDGE_TAB_WIDTH = 18 DIP`，高 `EDGE_TAB_HEIGHT = 48 DIP`

### 3.2 展开/收起右边缘像素零抖动守恒证明
在屏幕右侧吸附时，向左展开若直接改动 $x$ 坐标容易导致跳动。系统采用绝对锚点守恒算法：
- 设收起态锚点为 $(x_0, y_0)$，窗口右边缘绝对屏幕物理坐标为 $R = x_0 + W_{\text{collapsed}}$。
- 展开时：$x_{\text{expanded}} = R - W_{\text{expanded}} = x_0 + 80 - 370 = x_0 - 290$。
- 展开态右边缘：$x_{\text{expanded}} + W_{\text{expanded}} = (x_0 - 290) + 370 = x_0 + 80 = R$。
- **守恒结论**：展开与收起过程中，贴紧屏幕边缘的一侧物理像素位移 $\Delta R \equiv 0$。

### 3.3 边缘标签与侧栏锚点双向无损换算
- **侧栏 -> 边缘标签**（`calculateEdgeTabBounds`）：
  - 纵向居中对齐：$y_{\text{tab}} = y_0 + \left\lfloor \frac{H_{\text{collapsed}} - H_{\text{tab}}}{2} \right\rfloor = y_0 + \left\lfloor \frac{340 - 48}{2} \right\rfloor = y_0 + 146$。
  - 右吸附横向贴边：$x_{\text{tab}} = x_0 + 80 - 18 = x_0 + 62$。
  - 左吸附横向贴边：$x_{\text{tab}} = x_0$。
- **边缘标签 -> 恢复侧栏**（`restoreAnchorFromEdgeTab`）：
  - $y_0 = y_{\text{tab}} - 146$；
  - 右吸附：$x_0 = x_{\text{tab}} + 18 - 80$；左吸附：$x_0 = x_{\text{tab}}$。
- **无损证明**：$f^{-1}(f(\text{anchor})) \equiv \text{anchor}$，保证用户反复在侧栏与边缘标签间切换时，窗口纵向位置与所属显示器绝对零漂移。

### 3.4 拖拽与吸附策略
- `minibarWin` 监听 `move`/`moved` 事件，采用 90ms 防抖执行 `handleDragSettle()`；
- 支持 Windows 多显示器，通过光标所属显示器的 `workArea` 判定就近吸附（到左侧或右侧），并严格限制在工作区上下边界之内（`clampBoundsToWorkArea`），防止窗口飞出屏幕外或被任务栏遮挡；
- 按下拖动区域瞬间派发 `minibar:drag-start` IPC，立即通知主进程收起展开卡片，杜绝拖动时大方块悬空移动。

---

## 四、视觉系统与微积分切线连续性证明

针对上一轮反馈中“侧栏像大胶囊、直线中间有鼓包、图标看起来偏向桌面内侧”的问题，本轮重构从几何学与解析几何彻底重构：

### 4.1 几何区域严格划分
1. **原生窗口边界**：宽 80 DIP，高 340 DIP。
2. **黑色直线主体**：宽度固定为 **76 DIP**（长 252 DIP，高度 $y \in [44, 296]$）。
3. **中心线**：
   - **右吸附**：黑色主体位于 $x \in [4, 80]$，中心线为 $x = 4 + 76/2 = 42\text{ DIP}$；
   - **左吸附**：黑色主体位于 $x \in [0, 76]$，中心线为 $x = 0 + 76/2 = 38\text{ DIP}$；
   - 三个圆环中心、百分比中心、周期标签中心**严格在以该中心线对齐的容器中排布**，绝对不被上下端扩展曲线拉偏。

### 4.2 切线连续性证明（消除折角、鼓包与透明接缝）

以**右吸附**为例（贴靠屏幕右边缘 $x = 80$）：
面向桌面的直线段为 $x = 4$，$y \in [44, 296]$。
- **顶部反向凹弧**：起点 $(80, 0)$，终点 $(4, 44)$。
  三阶贝塞尔曲线控制点为：$P_0(80, 0)$，$P_1(44, 0)$，$P_2(4, 14)$，$P_3(4, 44)$。
  - 起点处一阶导数向量：$\vec{v}(0) = 3(P_1 - P_0) = 3(-36, 0) = (-108, 0)$，**纯水平向量，与屏幕上边缘 $y=0$ 严格相切（夹角 $0^\circ$）**；
  - 终点处一阶导数向量：$\vec{v}(1) = 3(P_3 - P_2) = 3(0, 30) = (0, 90)$，**纯垂直向下向量，与面向桌面的垂直直线 $x=4$ 严格相切（夹角 $90^\circ$）**；
  - 中间无任何折点、无鼓包，曲率平滑单调凹入。
- **底部反向凹弧**：起点 $(4, 296)$，终点 $(80, 340)$。
  控制点为：$P_0(4, 296)$，$P_1(4, 326)$，$P_2(44, 340)$，$P_3(80, 340)$。
  - 起点处导数：$3(0, 30) = (0, 90)$，纯垂直向下相切；
  - 终点处导数：$3(36, 0) = (108, 0)$，纯水平向右与屏幕下边缘严格相切。
- **SVG 完整路径**：
  - 右侧吸附：`M 80,0 C 44,0 4,14 4,44 L 4,296 C 4,326 44,340 80,340 L 80,0 Z`
  - 左侧吸附（严格镜像 $x' = 80 - x$）：`M 0,0 C 36,0 76,14 76,44 L 76,296 C 76,326 36,340 0,340 L 0,0 Z`

### 4.3 独立背景不透明度系统（35% ~ 100%）
- **绝不使用全局 CSS `opacity`**（否则文字、Logo、刻度环会变淡发虚）；
- 采用局部色彩 Alpha 映射：
  - 侧栏 SVG 填充：`fill="rgba(8, 8, 8, ${bgOpacity})"`
  - 详情卡背景：`backgroundColor: rgba(8, 8, 8, ${bgOpacity})`
  - 连接尖角边框：`borderColor: rgba(8, 8, 8, ${bgOpacity})`
  - 边缘微胶囊背景：`backgroundColor: rgba(8, 8, 8, ${bgOpacity})`
  - 文字、Logo、圆环色谱进度条保持 100% 饱和度与锐利对比度；
- 支持 35%~100% 滑块调节，防抖 500ms 持久化到 `localStorage`（`pulse_minibar_bg_opacity`）。

### 4.4 字体与阴影规范
- 统一使用 `font-sans`（优先匹配系统级平滑无衬线字体 Segoe UI Variable Text / Segoe UI / -apple-system）；
- 取消粗等宽字体，数字采用现代比例字体配合 `tabular-nums`，兼具数字等宽对齐与优美比例；
- 剥离大扩散外阴影（`box-shadow: none !important;`），杜绝桌面透明区域出现脏灰阴影边缘。

---

## 五、交互系统与状态机设计

### 5.1 双模式视图（边缘微胶囊 vs 完整侧栏）
- **微型胶囊模式（Edge Tab）**：
  - 宽 18 DIP，高 48 DIP，贴合屏幕边缘；
  - 内部小箭头指示展开方向（右吸附朝左 `◀`，左吸附朝右 `▶`）；
  - **位移阈值防误触**：记录按下时的屏幕坐标，在 `onPointerMove` 中计算欧几里得距离 $\Delta = \sqrt{dx^2 + dy^2}$。若 $\Delta > 4\text{px}$ 标记为拖拽，松手不触发恢复；仅在原位单击抬起时恢复完整侧栏。
- **完整侧栏模式（Full Sidebar）**：
  - 常态下 80 DIP 贴紧边缘；
  - 悬停圆环时触发向桌面内侧展开 264×264 DIP 单项详情卡。

### 5.2 详情卡防裁剪与流畅切换状态机
- **展开时**：先通知主进程将窗口物理拓宽到 370 DIP，在 `requestAnimationFrame` 下一帧触发详情卡 DOM 挂载与 CSS 位移淡入；
- **Agent 之间切换时**：卡片保持挂载与物理窗口宽度不变，直接瞬时切换内部数据，零闪烁、不先关后开；
- **收起时**：先将详情卡 CSS `opacity` 设为 0（160ms 优雅淡出动画），动画结束回调中卸载卡片 DOM，最后通知主进程将物理窗口收缩回 80 DIP。彻底解决旧版“窗口先收缩导致卡片被原生窗口边界生硬截断”的硬件裁剪缺陷。

### 5.3 右键上下文菜单与防误关
- 在侧栏、详情卡或边缘微胶囊非按钮区域右击，弹出右键菜单：
  1. `收起为边缘标签`
  2. `固定/取消固定当前详情卡`（无卡片时禁用）
  3. `背景不透明度`（内嵌精细滑块）
  4. `恢复默认外观`（重置为 94% 透明度并解绑 Pin）
  5. `完全隐藏悬浮窗`
- **防收起挂起**：右键菜单显示期间，挂起并清空 `leaveTimerRef`，鼠标在菜单上滑动不会导致卡片或悬浮窗收起。

---

## 六、详情卡数据分级与时间解析规范

详情卡尺寸扩展为 264×264 DIP，强化“额度”与“重置时间”双轴平衡：

```text
┌────────────────────────────────────────────────────────┐
│  (Logo) Claude     [Live]                   (Pin)  (X) │
├────────────────────────────────────────────────────────┤
│  5 小时额度                                             │
│  剩余 27%                          51 分钟后重置         │
│  ████████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  │
│                                      重置于 今天 18:40  │
│                                                        │
│  7 天额度                                              │
│  剩余 84%                        4 天 12 小时后重置     │
│  ██████████████████████████████████░░░░░░░░░░░░░░░░░░  │
├────────────────────────────────────────────────────────┤
│  ● Ready                                     Pulse 2.0 │
└────────────────────────────────────────────────────────┘
```

- **解析逻辑 (`agent-quota-utils.ts -> parseResetTimeInfo`)**：
  - 相对时间：将 `h` / `min` / `d` 转化为自然中文（如 `51 分钟后重置`、`4 小时 22 分钟后重置`、`2 天 7 小时后重置`）；
  - 绝对时间：从 `· 18:40` 解析为 `重置于 今天 18:40`；
  - 边界安全：若返回 `ready` 显示 `等待额度刷新`；若为 `null`、空字符串或 `--` 显示 `重置时间未知` 且绝对时间为 `null`，绝不虚构时间。

---

## 七、全套质量验证与测试报告

依照规约强制五步法，全部执行并通过：

```bash
1. git diff --check           # [PASS] 0 空白与格式错误
2. node --test test-minibar   # [PASS] 8 项独立针对性几何与数据测试
3. npm test                   # [PASS] 99 项全量自动化测试全部绿灯
4. npm run build              # [PASS] TypeScript 与 Vite 生产构建 0 错误
5. 敏感词扫描                  # [PASS] 0 泄露
```

### 独立测试用例明细 (`scripts/test-minibar-layout.mjs`)
1. **尺寸定义测试**：收起 80×340，展开 370×340。
2. **展开/收起锚点换算**：`calculateCollapsedBounds` 保证展开后仍可准确还原收起锚点。
3. **向左展开像素守恒**：右边缘像素在展开前后严格一致（例如绝对坐标均为 1920）。
4. **屏幕边界夹持测试**：任意越界拖动均被限制在屏幕可见工作区内。
5. **额度保真度测试**：已用与剩余百分比严格映射，缺失数据保持 `null`，不冒充 0% 或 100%。
6. **敏感词隔离扫描**：正则扫描所有核心源码，无任何密钥、凭据或物理地址残留。
7. **边缘微胶囊换算守恒**：`calculateEdgeTabBounds` 与 `restoreAnchorFromEdgeTab` 双向无损映射。
8. **中文时间解析测试**：相对倒计时、绝对时间点、未知边界条件的输出严格符合预期。

---

## 八、提交报告强制五问对账

| 序号 | 审查问题 | 事实对账结论 |
| :--- | :--- | :--- |
| 1 | **当前功能是否连接真实设备？** | **否**。当前任务属于悬浮窗 UI、动效、几何吸附及窗口管理优化，不连接手环物理设备。 |
| 2 | **当前功能是否调用真实 Core？** | **否**。数据源通过主进程已有 `StatusServer` 和 `SessionManager` 轮询和事件推送，不修改 Core 也不调用 Core。 |
| 3 | **当前是否存在 Mock？** | **否**。额度百分比、倒计时和活动会话均消费真实推送；缺失数据时明确标记未知或未提供，无任何造假数据。 |
| 4 | **哪些状态仍然只是占位状态？** | 若官方上游暂未支持某 Agent 的 7 天长周期额度，卡片客观显示为“官方未提供此周期额度”。 |
| 5 | **下一阶段缺少什么能力？** | 本次悬浮窗所有 5 项定向改造已全部闭环验收。下一阶段可按版本计划推进快应用表盘数据同步。 |

---

## 九、交付报告四分类清晰隔离

### 9.1 Implemented（真实完成内容）
- 宽度固定 76 DIP 的黑色主体与数学级切线连续反向凹弧轮廓（左右吸附准确镜像）；
- 三个圆环、百分比、周期与中心线严格共线对齐；
- 5 项功能原生毛玻璃右键上下文菜单（支持菜单打开期间暂停收起）；
- 宽 18 DIP、高 48 DIP 边缘胶囊标签视图，带 4px 欧氏距离防误触拖动/单击恢复；
- 35%~100% 独立背景不透明度调节，文字/圆环保持 100% 锐利，防抖 500ms 持久化；
- 详情卡 264×264 尺寸，左侧大字剩余百分比（26 DIP）+ 右侧中文倒计时（16 DIP）+ 进度条 + 辅助行绝对时间；
- 8 项针对性单元测试与全量 99 项自动化测试通过，生产构建零错误。

### 9.2 Mock / Placeholder（模拟或占位内容）
- 无硬编码假数据。

### 9.3 Not implemented（尚未完成内容）
- 无（用户本轮提出的 5 项定向要求已 100% 覆盖）。

### 9.4 Next step（下一阶段任务）
- 交付给 DeepSeek 审核或实机启动体验。
